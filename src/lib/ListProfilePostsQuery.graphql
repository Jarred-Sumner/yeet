#import "./PostListItemFragment.graphql"

query ListProfilePosts($profileId: ID!, $limit: Int, $offset: Int) {
  profile(id: $profileId) {
    posts(limit: $limit, offset: $offset) {
      offset
      limit
      hasMore

      data {
        ...PostListItemFragment
      }
    }
  }
}
