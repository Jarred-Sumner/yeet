#import "./ViewThreadFragment.graphql"

mutation CreatePostThread(
  $mediaId: ID!
  $blocks: JSON
  $body: String
  $nodes: JSON
  $format: String!
  $strings: [String!]
  $autoplaySeconds: Int
  $bounds: RectangleInputObject!
  $colors: ColorsInputObject!
  $examples: JSON
  $remixId: ID
  $postOffset: Int = 0
  $postsCount: Int = 2
  $editToken: String
  $layout: String
) {
  createPostThread(
    mediaId: $mediaId
    blocks: $blocks
    nodes: $nodes
    body: $body
    remixId: $remixId
    examples: $examples
    format: $format
    stringList: $strings
    autoplaySeconds: $autoplaySeconds
    bounds: $bounds
    colors: $colors
    editToken: $editToken
    layout: $layout
  ) {
    createdAt

    ...ViewThreadFragment
  }
}
