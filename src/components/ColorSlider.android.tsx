import * as React from "react";
import { View } from "react-native";
import Animated from "react-native-reanimated";

export const ColorSlider = Animated.createAnimatedComponent(View);
export default ColorSlider;
